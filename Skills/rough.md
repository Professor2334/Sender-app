# Edit the following content in the head

- include seo-meta  in the head 
- add favicon to the head 
- put a description in the head

# Edit the following content in the header
- place the navigation menu in the center 
- remove the bottom border line of the header
- add favicon to the header
- remove the login link from the header and only have the get started button
- confirm that typography in the header is making use of the inter font family
- remove the brand color in the sendsignal in the header and make use of the neutral color
- add horizontal padding of 6rem in the header
- ask the agent to configure the box-sizing: border -box in the global settings of the page
- use the body large text for the nav links

# Edit the following in the  hero section
- remove every section below the hero section including the footer
- remove the border bottom line of the hero section
- implement a 100vh height for the whole marketing page
- remove the border top line of the simple scalable pricing 

# Signup page - experience problem
- input fields need hover interaction states

# signup page - UI problem
- reduce default border blackness of the input field 
- reduce the blackness of the label text 
- remove all placeholders
- change form title to signup
- remove form description
-  change form and button  text to create account
- add animation to input focus 
- cursor behavior 

# form states

- empty input states

# company name field
- prevent  special charcters
- minimum of 2 characters 
- if a user goes into focus mode and leaves without typing any thing ,display an error using the error color role saying " filed must not be empty"remove the error only when they start typing in the input field 

- Display "Enter a valid company email address" the moment the user starts typing in the email field. Remove the error message only when the extension in the email address that follows the @ symbol does not end with "gmail.com" or "yahoo.com".

- For the password requirements display this in real time
* Password must be 8 characters
* Password must contain a number
* Password must contain a special character

Do not display all the password requirements at once. Display them as the user types in the password field and as they meet each requirement, display a green checkmark next to it.

on the create account form when a user enter a valid field,change the background color of the input field to primary container color

# onboading phase 
- step 1 - welcome to sendsignal,and a title head saying let get you start with a short description beneath it,and a button indicating to Next  button  with an arrow icon that will take user to step 2,
- remove the top line  border of the onboarding page
